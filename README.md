# Heart-Disease-Prediction
This project predicts heart disease severity using the Heart Disease UCI dataset. ML-based data cleaning (KNN, Random Forest), SMOTE for class balance, and preprocessing techniques were applied. Models like XGBoost, SVM, and Random Forest were trained and evaluated. SHAP analysis was used for explainable predictions.
